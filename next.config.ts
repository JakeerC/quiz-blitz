import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  reactStrictMode: true,
  // Configure port to match Vite config (3000)
  // This is handled by the dev script, not here
};

export default nextConfig;
